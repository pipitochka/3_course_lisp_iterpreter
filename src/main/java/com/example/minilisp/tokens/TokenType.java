package com.example.minilisp.tokens;

public enum TokenType {
    LPAREN, // done
    RPAREN, // done
    QUOTE, // done
    NUMBER, // done
    STRING, //done
    BOOLEAN, //done
    VARIABLE, //done
    SPECIAL_FORM, //done
    FUNCTION,
    NIL,
    COMMENT,
    OPERATOR
}
