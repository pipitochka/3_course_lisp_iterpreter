1. Исправить количество исключений + Добавить в токены позиции, чтобы отслеживать место ошибки
2. Что-то придумать с транжирством памяти при создании токенов
3. Поправить для NILL чтобы он был статиком
4. Поправить чтобы печать нормальная была для expression
5. Сгребание если . встретили (все превратим в один после точки)
6. ```
(def \n "
")
(def acc-class (lambda ()
  (do
  (def get (dambda () val))
  (def add (dambda (delta)
    (do
      (set! val (_+_ val delta))
      val
    )))
  (def sub (dambda (delta)
    (do
      (def new (_-_ val delta))
      (if (_<_ new 0) (print (_++_ "аяяй" \n)) (set! val new))
      val
    )))
  (def acc-constructor (lambda (val) (lambda (method-name) (eval method-name))))
  acc-constructor
)))
```
7. Добавить . чтобы забивать на дальнейшие параметры